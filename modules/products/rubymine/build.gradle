plugins {
    id "org.jetbrains.intellij"
}

intellij {
    version = platformVersion
    plugins = ["org.jetbrains.plugins.ruby:$versionIdeaBuild"]
}

dependencies {
    implementation project(":envfile-platform")
}
